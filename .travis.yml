sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t crobdocker/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true crobdocker/docker-react npm run test -- --coverage
